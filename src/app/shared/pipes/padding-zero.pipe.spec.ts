import { PaddingZeroPipe } from './padding-zero.pipe';

describe('PaddingZeroesPipe', () => {
  it('create an instance', () => {
    const pipe = new PaddingZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
