import { EventIntensityPipe } from './event-intensity.pipe';

describe('EventIntensityPipe', () => {
  it('create an instance', () => {
    const pipe = new EventIntensityPipe();
    expect(pipe).toBeTruthy();
  });
});
