export interface EventData {
  name: string;
  location: [number, number];
  startDate: string;
  endingDate: string;
  description: string;
  sport: number;
  intensity: string;
  fee: number;
  currencyCode: string;
  maxPlayers: number;
}
